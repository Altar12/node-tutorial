const amount = 9;
const reference = 10;

if (amount < reference)
    console.log(`${amount} is less than ${reference}`);
else
    console.log(`${amount} is greater than or equal to ${reference}`);

console.log('My first node app!');